# 问题

### reactivity
##### effect.ts 
- `ReactiveEffect` 的 `deps` 为什么是数组，不怕重复吗

# 收获
### reactivity
##### effect.ts
- 副作用函数收集到依赖关系之后可以由 `scheduler` 来触发更新机制
- 副作用函数 `watchEffect` 返回的 `StopHandle` 调用时，解除 `ReactiveEffect` 和 `dep` 依赖关系